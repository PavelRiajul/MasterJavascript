
// intro to variables

// variables can store some information
// we can use that information later
// we can change that information later

// declare a variable
  var  firstName = "Riajul islam";
//variable declare(var keyword)  variable name(firstName) = variable information store (Riajul islam)

// use a variable
console.log(firstName)

// change value
firstName = "Imadul islam";
console.log(firstName)
